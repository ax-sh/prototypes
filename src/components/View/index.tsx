import React from "react";
import styled from "styled-components";

const ViewStyled = styled.section``;
const View = (props:any) => {
  return <ViewStyled {...props}>View</ViewStyled>;
};

export default View;
